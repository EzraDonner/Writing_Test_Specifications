const multiply = (a, b) => {
  return a * b;
};

// Expect multiply(2, 5). output should be 10
// Expect multiply(2, -5), output should be -10
// Expect multiply("x", 3), output should be an error
// Expect multiply("", 3), output should be error

function concatOdds (arr1, arr2){
    let result = [];
}

const array1 = [1, 2, 3, 4, , 9, 11, 43, -6];
const array2 = [-2, 5, 9, 2, 0, 1, 12, 50];

function concatOdds(arr1, arr2) {
  let result = []
  for (let i = 0; i < items.length; i++)
}

// Expect output to be one array of odd numbers
// Expect to be all unique numbers, no repeats
// Expect output to be only integers
// Expect strings, decimals, fractions, empty input, booleans, to output an error

// When a user's cart is empty, they should get a message saying, "Your cart is empty."
// When a user clicks "Checkout" with at least one item in their cart, it will take them to another page with three options: Continue as Guest, Create a New Account, or Log In.
// If a user continues as Guest, they should be prompted to enter First Name, Last Name, Mobile Number, Email Address, Billing Information, and Shipping Information, and accept the terms of service.
// If a User tries to create a new account with existing credentials, they should get a message stating, "There's already an account with this email. Sign in or learn more."
// Once information is entered, user will be prompted to view cart before Confirming payment.
// User will Confirm Payment.
// Final page should be Confirmation page with order number.

// If user creates a new account, they will be prompted to input name, contact, billing, and shipping, and payment information.
// If user enters incorrect credentials, they will get an error message stating, "One of the fields below is incorrect."

// If logged in with correct credentials, user will review cart and Place Order.